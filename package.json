{
  "name": "fofbiomass",
  "description": "FoFBiomass: geospatial mapping of SPAM2020 crop production across all crops",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "colormap": "^2.3.2",
    "ol": "^10.7.0"
  },
  "eslintConfig": {
    "extends": "openlayers",
    "rules": {
      "no-unused-vars": [
        "error",
        {
          "varsIgnorePattern": "^map"
        }
      ]
    }
  },
  "scripts": {
    "lint": "eslint main.js",
    "test": "npm run lint",
    "start": "vite serve",
    "build": "vite build",
    "dev": "vite"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "eslint-config-openlayers": "^19.0.0",
    "vite": "^7.2.2"
  }
}
